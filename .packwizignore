# Files that live in this repository but are NOT part of the modpack.
#
# Without this, `packwiz refresh` indexes the repo's own metadata and the installer
# then copies AGENTS.md, the CI workflows and the README into every player's Minecraft
# directory. Everything listed here is repo furniture, not pack content.

# Repository metadata
.github/
.git/
.gitignore
.gitattributes
.editorconfig
.packwizignore

# Documentation and licensing
*.md
UNLICENSE
LICENSE
NOTICE

# Local build / test output (also in .gitignore)
/server/
/build/
/out/
*.mrpack
*.zip
